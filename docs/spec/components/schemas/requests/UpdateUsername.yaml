type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - type
      - attributes
    properties:
      type:
        type: string
        enum: [ update_account_username ]
      attributes:
        type: object
        required:
          - new_username
        properties:
          new_username:
            type: string
            description: The account's username.
