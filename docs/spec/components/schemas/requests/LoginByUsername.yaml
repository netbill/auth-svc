type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - type
      - attributes
    properties:
      type:
        type: string
        enum: [ login_by_username ]
      attributes:
        type: object
        required:
          - username
          - password
        properties:
          username:
            type: string
            description: The account's username.
            example: example
          password:
            type: string
            format: password
            description: The account's password.
            example: StrongP@ssw0rd!