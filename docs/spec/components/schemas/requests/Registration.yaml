type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - type
      - attributes
    properties:
      type:
        type: string
        enum: [ registration_account ]
      attributes:
        type: object
        required:
          - email
          - password
          - username
        properties:
          email:
            type: string
            format: email
            description: The account's email address.
            example: example@gmail.com
          password:
            type: string
            format: password
            description: The account's password.
            example: StrongP@ssw0rd!
          username:
            type: string
            description: The account's username.
            example: example
