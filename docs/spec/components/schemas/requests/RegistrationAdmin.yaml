type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - type
      - attributes
    properties:
      type:
        type: string
        enum: [ registration_account_by_admin ]
      attributes:
        type: object
        required:
          - email
          - password
          - username
          - role
        properties:
          email:
            type: string
            format: email
            description: The account's email address.
            example: example1312@gmail.com
          password:
            type: string
            format: password
            description: The account's password.
            example: StrongP@ssw0rd!
          username:
            type: string
            description: The account's username.
            example: admin
          role:
            type: string
            description: The role assigned to the registering account.
            enum: [ admin, moderator ]
            example: admin
