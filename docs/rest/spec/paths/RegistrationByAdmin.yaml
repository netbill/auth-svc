post:
  tags:
    - registration
  summary: Register a new admin user
  description: >
    Registers a new user by an authenticated admin.

  security:
    - BearerAuth: [ ]
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../components/schemas/requests/RegistrationAdmin.yaml'
  responses:
    '201':
      description: User successfully registered by admin
      content:
        application/json:
          schema:
            $ref: '../components/schemas/responses/User.yaml'

    '400':
      description: >
        Bad Request. Request body is invalid or some attributes do not meet requirements.
        Check the `errors` array for details.
      content:
        application/json:
          schema:
            $ref: '../components/schemas/responses/Errors.yaml'

    '401':
      description: >
        Unauthorized. User cannot be resolved from context.
        Check the `detail` field in the response for more information.
      content:
        application/json:
          schema:
            $ref: '../components/schemas/responses/Errors.yaml'

    '409':
      description: >
        Conflict. Email is already taken.
        Check the `detail` field in the response for more information.
      content:
        application/json:
          schema:
            $ref: '../components/schemas/responses/Errors.yaml'

    '500':
      description: Internal Server Error
      content:
        application/json:
          schema:
            $ref: '../components/schemas/responses/Errors.yaml'
